AIレビューをする際の基盤を作成する。
Codex, githubだけでは完全レビューの自動化を行うことができない（OpenAiのAPIを使用すれば可能だが有料）。そのため、現在は差分を取得するスクリプトをlocalで実行し、それをもとにreviewを行っている。

・実装レビュー
基本的には、変更前の状態と、変更をcommitし、最新のHEADとの差分を取得し、その差分diff, 機能作成指示書（function.md）等をAIに渡した上でレビューを行う。

・テストレビュー
後述

・メタレビュー
これを最終的には定期的に行えるようにしたい。メソッドは不明のため後述
